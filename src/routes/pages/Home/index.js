import { StyledBanner } from "./Home.styled";

const Home = () => {
  return (
    <section>
      {/* Banner components */}
      <StyledBanner>
        <h1>Farhan Nur Hidayat</h1>
        <span>Front-end Developer</span>
      </StyledBanner>
    </section>
  );
};

export default Home;
